<% provide(:title, 'Sign up') %>

<div class="row">
  <div>
    <%= form_for(@user) do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.email_field :email %>

      <%= f.submit "Create" %>

    <% end %>
  </div>
</div>